[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mammo-clip"
version = "0.1.0"
description = "Mammo-CLIP"
requires-python = "==3.8.18"

dependencies = [
]
